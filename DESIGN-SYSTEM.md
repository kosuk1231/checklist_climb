# 등반대회 운영 도구 — 디자인 시스템

세 페이지(`index.html` 체크리스트 · `guide.html` 당일 안내 · `admin.html` 관리)가 공유하는 규칙.
`public/theme.css` 가 이 문서의 구현체입니다. 화면을 새로 만들 때는 여기 정해진 것만 쓰고,
없는 값이 필요하면 이 문서를 먼저 고칩니다.

---

## 1. 이 제품이 무엇인가

| 항목 | 내용 |
|---|---|
| 도메인 | 비영리 협회 행사 운영(현장 오퍼레이션) |
| 사용자 | 사무처 직원 18명, 권익위원 10명, 스탭·연대회의 |
| 사용 환경 | **9월 새벽 7시 야외**, 직사광선, 장갑·짐을 든 한 손 조작, 통신 불안정 |
| 기기 | 대부분 개인 스마트폰 (iOS/Android 혼재), 일부 노트북 |
| 사용 시간 | 한 번에 5~20초. 확인하고 바로 닫음 |
| 실패 비용 | 물품 하나 누락 = 1,470명 앞에서 사고. 되돌릴 수 없음 |

**설계 판단의 기준선**: 예뻐 보이는 것보다 *새벽에 급하게 봐도 틀리지 않는 것*.
그래서 아래 세 가지가 다른 모든 취향에 우선합니다.

1. 텍스트 대비 — 햇빛 아래 저밝기 화면에서도 읽힐 것
2. 터치 목표 — 장갑 낀 손으로 잘못 누르지 않을 것
3. 상태 명확성 — 지금 무슨 날짜/누구 기준인지 항상 화면에 보일 것

**하지 않기로 한 것**: 그라데이션, 다중 그림자, 색만으로 구분되는 정보,
호버에만 나타나는 기능, 화면 전환 애니메이션, 이모지 아이콘.

---

## 2. 색

행사 정체성(등반·숲)에서 온 녹색 계열 한 축 + 기능색 최소.

### 면

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg` | `#f3f6f4` | 페이지 배경. 녹색이 아주 옅게 섞인 회색 |
| `--card` | `#ffffff` | 카드·목록·입력 |
| `--card-2` | `#f6f9f7` | 카드 안 보조 블록(메모, 완료 행) |
| `--line` | `#dfe6e1` | 구획 구분선. 장식용이라 대비 기준 없음 |
| `--line-2` | `#7f9488` | **입력·버튼 테두리.** 흰 배경 대비 3.23:1 |

### 글자

| 토큰 | 값 | 흰 배경 대비 | 용도 |
|---|---|---|---|
| `--ink` | `#12231a` | 16.4:1 | 제목, 항목 이름 |
| `--text` | `#2f4238` | 10.7:1 | 본문 |
| `--muted` | `#5a6d61` | 5.5:1 | 보조 정보(수량, 담당, 라벨) |
| `--faint` | `#74867a` | 3.9:1 | 비활성·플레이스홀더 **전용**. 읽어야 하는 정보에 쓰지 않음 |

### 강조

| 토큰 | 값 | 용도 |
|---|---|---|
| `--accent` | `#1e7a4b` | 담당자 이름, 활성 탭, 주요 버튼, 완료 표시 |
| `--accent-ink` | `#135c37` | 연한 녹색 배경 위 글자(7.1:1) |
| `--accent-soft` / `--accent-line` | `#e7f4ec` / `#b5dcc4` | 강조 배경·테두리 |
| `--now` | `#c2410c` | 진행 중 시간대. 녹색의 보색이라 눈에 먼저 들어옴 |
| `--warn` | `#a16207` | 데모 모드 등 경고 |
| `--danger` | `#b91c1c` | 삭제·초기화 |

### 체크리스트 3단계

날짜를 헷갈리면 전체가 무너지므로 **단계마다 다른 색**을 씁니다.

| 단계 | 토큰 | 값 |
|---|---|---|
| 9/10 패킹 | `--s0` | `#2563eb` |
| 9/11 확인 | `--s1` | `#a75607` |
| 9/12 반입 | `--s2` | `#bf1b1b` |

선택된 탭·체크박스·진행바·숫자가 모두 이 색으로 함께 바뀝니다.
다른 두 날짜의 상태 점은 항상 `--accent`(녹색)이라, 단계색과 완료색이 섞이지 않습니다.

### 대비 검증

`--line`(장식선)을 제외한 모든 조합이 WCAG 2.1 AA를 통과합니다.

```
ink/card      16.39    text/card     10.73    muted/card     5.53
faint/card     3.86    accent/card    5.33    accent-ink/soft 7.10
흰글자/accent  5.33    흰글자/s0      5.17    흰글자/s1       5.28
흰글자/s2      6.20    line-2/card    3.23  (1.4.11 비텍스트 3:1)
```

색 값을 바꾸면 이 표를 다시 계산하고 문서를 고칩니다.

---

## 3. 타이포그래피

**Pretendard Variable 한 종류만.** `public/fonts/` 자체 호스팅(다이내믹 서브셋).
CDN을 쓰지 않는 이유는 협회·구청 네트워크가 외부 CDN을 막는 경우가 있어서입니다.

| 역할 | 크기 / 굵기 | 비고 |
|---|---|---|
| 페이지 제목 | 18px / 700 | `letter-spacing: -.02em` |
| 부제 | 12.5px / 500 | `--muted` |
| 카드 제목 | 15.5px / 700 | |
| 항목 이름 | 15.5px / 600 | `word-break: keep-all` (한글 어절 보존) |
| 본문 | 15.5px / 400 | 기본 `line-height: 1.6` |
| 보조 정보 | 13.5px / 400~600 | |
| 라벨·배지 | 12~12.5px / 600 | |

**13px 미만은 쓰지 않습니다.** 야외에서 안 읽힙니다.
숫자가 나열되는 곳(시간, 카운트, 진행률)은 `font-variant-numeric: tabular-nums`로
자리를 고정해 값이 바뀌어도 흔들리지 않게 합니다.

---

## 4. 간격·형태

- 리듬: 4px 배수. 실사용 값은 `4 / 6 / 8 / 10 / 12 / 14 / 18 / 22`
- 화면 좌우 여백: `clamp(16px, 4.5vw, 22px)`
- 모서리: 카드 `12px`, 입력·버튼 `8px`, 배지·칩 `999px`
- 그림자: `0 1px 2px rgba(18,35,26,.05)` **한 종류만**. 떠 보이게 하려고 겹치지 않음
- 최대 폭: 720px (관리자 900px)

### 터치 목표

| 요소 | 최소 크기 |
|---|---|
| 체크박스 히트영역 | 48×48 |
| 버튼·입력 | 높이 42 |
| 칩·작은 버튼 | 높이 34 |
| 하단 탭 | 높이 62 + safe-area |

체크박스는 보이는 사각형이 26px이지만 히트영역은 48px입니다.
장갑 낀 손과 흔들리는 화면을 감안한 값입니다.

---

## 5. 컴포넌트 규칙

**카드** — 흰 배경, 1px 테두리, 12px 모서리, 그림자 1단계. 카드 안에 카드를 넣지 않습니다.

**목록** — 카드 하나 안에 `<li>`를 쌓고 사이를 1px 선으로 나눕니다. 항목마다 카드를 쪼개지 않습니다(스크롤이 두 배가 됨).

**배지** — 상태는 색과 **글자를 함께** 씁니다. "내 담당" 배지는 녹색이면서 글자도 있습니다. 색맹 사용자를 위한 조치이자, 햇빛에 색이 날아갈 때의 보험입니다.

**빈 상태** — 점선 테두리 + 굵은 한 줄 + 다음 행동 안내. "결과 없음"으로 끝내지 않습니다.

**로딩·저장** — 화면을 가리는 스피너를 쓰지 않습니다. 체크는 먼저 반영하고(낙관적 갱신) 실패하면 되돌리며 토스트로 알립니다. 통신이 불안정한 현장이라 기다리게 하면 두 번 누릅니다.

**포커스** — 모든 조작 요소에 `outline: 2px solid`. 관리자 페이지를 키보드로 쓰는 경우가 있습니다.

---

## 6. 페이지별 성격

| 페이지 | 성격 | 특징 |
|---|---|---|
| `index.html` 체크리스트 | 조작 도구 | 단계색이 화면 전체를 지배. 큰 체크박스. 정보 밀도 높게 |
| `guide.html` 당일 안내 | 읽는 문서 | 녹색 단일 강조. 타임라인 레일. 읽기 편한 행간 |
| `admin.html` 관리 | 편집 도구 | 넓은 폭(900px). 인라인 편집. 위험 동작은 확인창 |

---

## 7. 접근성 체크리스트

새 화면을 만들 때 확인합니다.

- [ ] 본문 대비 4.5:1, 큰 글씨·아이콘·테두리 3:1 이상
- [ ] 정보를 색으로만 전달하지 않음 (글자·아이콘 병기)
- [ ] 터치 목표 44px 이상
- [ ] `<h1>` → `<h2>` 순서가 건너뛰지 않음
- [ ] 아이콘 전용 버튼에 `aria-label`
- [ ] 탭·토글에 `aria-selected` / `aria-pressed`
- [ ] 목록 갱신 영역에 `aria-live="polite"`
- [ ] `prefers-reduced-motion` 존중
- [ ] 키보드만으로 전체 조작 가능
- [ ] `env(safe-area-inset-*)` 적용 (아이폰 하단 바)

---

## 8. 참고 자료

`bradtraversy/design-resources-for-developers` 계열 목록에서 이 프로젝트가 실제로 쓴 것.

| 용도 | 선택 | 이유 |
|---|---|---|
| 한글 폰트 | Pretendard Variable (SIL OFL) | 한글 자소 균형이 좋고 가변 폰트라 굵기 파일이 하나 |
| 아이콘 | 인라인 SVG 직접 작성 | 5개뿐이라 아이콘 라이브러리를 부르면 손해 |
| 색 대비 검사 | WCAG 상대휘도 직접 계산 | 스크립트로 검증해 이 문서에 기록 |
| CSS 프레임워크 | 사용 안 함 | 3페이지 단일 CSS. Tailwind 빌드 단계가 배포를 복잡하게 만듦 |

**의도적으로 쓰지 않은 것**: 컴포넌트 라이브러리(shadcn/MUI), 아이콘 폰트, 애니메이션 라이브러리,
CSS-in-JS. 빌드 없이 정적 파일만으로 Vercel에 올라가는 구조를 유지하기 위해서입니다.
1년 뒤 다른 담당자가 열어도 CSS 파일 하나만 보면 됩니다.
